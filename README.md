## How to get the app up and running for development

Navigate to `/server` and type

```bash
yarn dev
```

Then, while that's running, in a separate tab/window, at the root of the project, run

```bash
yarn start
```
